# Imagem base leve do Node
FROM node:20-alpine

# Evita problemas de fuso/locale em logs
ENV TZ=America/Fortaleza

# Dependências extras que ajudam no hot reload / Turbopack
RUN apk add --no-cache libc6-compat bash git curl python3 make g++

# Pasta de trabalho
WORKDIR /app

# Copia só dependências primeiro
COPY package*.json ./

# Instala dependências
RUN npm install

# Variáveis padrão
ENV HOST=0.0.0.0
ENV PORT=3000
EXPOSE 3000

# Em dev, o código é montado via volume
